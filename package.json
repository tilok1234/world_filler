{
  "name": "world-filler",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic world director: compiles gameplay content packs over WorldForge world artifacts.",
  "type": "module",
  "engines": {
    "node": ">=24.15.0 <25"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test \"dist/tests/*.test.js\"",
    "fill": "node dist/src/cli.js"
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "typescript": "7.0.2"
  }
}
